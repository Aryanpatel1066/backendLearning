test is name of db => multiple collection => multiple documents

cls to clear the screen

show databases ; (show dbs ;) => show the how many db

if mongodb when show databases when it have data

// how to show database content

* use admin (admin is db)

// to create new database
* use newdb ;

//* show collection ;
 hear db represent the current database like ex newdb> db.student 

create connection * db.createCollection("student")

update collection * db.student.renameCollection("student_new")

remove collection db.student.remove({});

db.student_new.drop();

//successfully install mongodb


// today we talk learn about soap (simple object access protocol)

//rest api => represent state transfer 2 application communicate together in different format file

=> change one stage to another create read update delete (crud)

uri unique resource identifier => ipaddress,portnumber/nameofapp/versonnumber/reosucename/

vehicle id : 4351 

111.21.31.42:8181/vehiclestore/v1/vehicalev1/4351